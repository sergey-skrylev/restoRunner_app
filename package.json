{
  "name": "food-runner",
  "version": "0.0.1",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "cd server && npm install && npx sequelize db:migrate:undo:all && npx sequelize db:migrate && npx sequelize db:seed:all && node app.js "
  },
  "license": "ISC",
  "engines": {
    "node": "14.x"
  },
  "dependencies": {
    "react-toastify": "^8.1.0"
  }
}
