// import searchAT from '../actionTypes/searchAT';

// const initialState = {
//   mySearch: [],
// };

// export const searchReducer = (state = initialState, action) => {
//   switch (action.type) {
//     case searchAT.INIT_SEARCH:
//       return { ...state, mySearch: action.payload };
//     default:
//       return state;
//   }
}
